Layouts & Systems Draftsman Toolkit - AI Agent Update Workflow
2: Last Update Verification: April 02, 2026 - 11:15 AM

This workflow is the absolute source of truth for AI agents interacting with this repository: https://github.com/Japzon/Layouts-Systems-Draftsman-Toolkit-Addon.git
Follow these exact, descriptive steps meticulously to robustly update or create new features, preserving the addon's unique framework and preventing major structural or memory errors across future Blender 4.5.6 LTS updates.

Category Index: {
1.  Strict Operational Protocols (Mandatory)
2.  Literal Naming Constraints and Structure Enforcement
2.1 Standardized Naming Conventions (Mandatory)
3.  Core Architecture Rules (Strict Separation of Concerns)
4.  Feature Creation and Extension Sequence
4.1 Timer-based Update Discipline (Mandatory)
4.2 Debugging and Console Discipline (Mandatory)
5.  Procedural Safety Limitations
6.  Code Skeleton Standards (generators.py)
7.  Naming Transition & Extension Compliance
8.  Current Session Tasks Log Here
}

1. Strict Operational Protocols (Mandatory)
* Enforce Strict Scope Adherence: Never add, alter, refactor, or remove features unless explicitly instructed by the user. Unprompted optimizations often break delicate procedural workflows. Focus exclusively on the execution of the given task.
* Prioritize High-Density Information: Substantially bypass conversational filler and pleasantries. Prioritize comprehensive checklists, provide compact summaries of modifications, and output code with clear inline documentation to ensure rapid comprehension.
* Interpret Task Syntax Accurately: Respect the user's specific punctuation-based task syntax. A period followed by a comma (.,) indicates a completely new, isolated task. A single period (.) indicates a direct continuation or sub-step of the previously stated objective.
* Ensure 4.5.6 LTS Compliance: All Python code must strictly adhere to the Blender 4.5.6 LTS API. You must utilize modern type hinting (e.g., bpy.types.Object), explicitly manage BMesh memory, and ensure proper bpy.context passing, as implicit context evaluation is deprecated and unstable.
* Delete One-Time Execution Files After Successful Completion (Mandatory): Any temporary helper scripts created solely to execute a patch, migration, or one-time data transformation (e.g., patch_*.py, fix_*.py, migrate_*.py placed in _dev/) must be deleted immediately after the task they were created for has been verified as successfully completed. Leaving these files in the repository constitutes permanent clutter and must never be done.
* Preserve User Context: Operators launched from Edit Mode must return the user to Edit Mode upon completion (unless the feature's core logic explicitly dictates otherwise). Capture and restore the initial mode (e.g., `initial_mode = context.mode`) to prevent disruptive mode switches during modeling workflows.
* No Automatic Commits (Strict): Do NOT perform `git commit` or `git push` operations automatically. Only execute these commands when explicitly requested by the user to finalize a specific, verified task.
* Avoid Terminal and Executable Code Edits: Avoid using terminals and executable files to edit the addon code to avoid untraceable changes and lessen corruption frequency when reverting a prompt.

2. Literal Naming Constraints and Structure Enforcement
* Avoid Generic Variable Reuse: The codebase strictly prohibits generic variable reuse (e.g., using radius for gears, wheels, and springs) to prevent cross-contamination of parametric data. Use uncompromising, literal naming.
* Mirror GUI Labels in Property Variables (properties.py): Variable names must precisely match the literal string label shown to the user in the frontend GUI. Do not attempt resource efficiency by reusing properties across distinct mechanical categories. You must explicitly use gear_radius, rack_width, wheel_base_radius, or motor_radius depending on the active context.
* Synchronize GUI Classes in Panel Files: Panel class names must perfectly mirror their string display tag while adhering to Blender's structural naming conventions (PREFIX_PT_name). For example, a panel displaying the text "Mechanical Presets" strictly dictates that the underlying Python class title must be LSD_PT_Mechanical_Presets.
* Trace GUI Identity in Operators (operators.py): Action classes and bl_idname identicality must logically trace the GUI text using Blender's operator conventions (PREFIX_OT_name). For a button displaying "Generate Robot with AI", the operator class must be LSD_OT_Generate_Robot_With_AI and its identifier must be lsd.generate_robot_with_ai.

2.1 Standardized Naming Conventions (Mandatory)
* Operators: PREFIX_OT_Description (e.g., LSD_OT_Apply_Joint_Settings).
* Panels: PREFIX_PT_Description (e.g., LSD_PT_Kinematics_Setup).
* PropertyGroups: PREFIX_PG_Description (e.g., LSD_PG_Kinematic_Props).
* UILists: PREFIX_UL_Description (e.g., LSD_UL_Mat_List).
* Scene Properties: lsd_lowercased_name (e.g., lsd_active_rig).
* Bone/Object Properties: lsd_pg_internal_props (Pointer to PG).
* Member Properties: lowercased_literal_name (e.g., joint_type).

3. Core Architecture Rules (Strict Separation of Concerns)
Know exactly where Python logic belongs before writing code. You must never mix module responsibilities. Note: BMesh generation logic is strictly forbidden in properties.py to prevent circular import loops.
 * __init__.py: Handles addon metadata, imports all submodules, and manages the central registration/unregistration loops via bpy.utils.register_class.
 * properties.py: Contains only Add-on PropertyGroup classes containing bpy.props declarations (e.g., LSD_PG_Mech_Props and LSD_PG_Inertial_Properties).
 * generators.py (New Mandate): Contains all procedural BMesh generation logic, mathematical plotting, and mesh construction functions. This isolates heavy geometry calculations from the data model.
 * operators.py: Holds Button execution classes. These classes parse the properties, call functions from generators.py or core.py, and contain the execute() methods that trigger core actions. You must explicitly pass bpy.context into your wrapper logic safely.
 * core.py: Holds pure utility and backend functions like Gizmo updates, Rigging application, Matrix math handlers, and Material assignments that do not directly generate meshes or draw to the GUI.
 * panels/ (UI Files): Contains purely separated logic grouping layout drawing functions like the draw() definitions. No data mutation should ever occur here.
 * Iterative Synchronization Execution: All operators and property update callbacks must robustly iterate through all selected bones (in Pose Mode) or selected objects (in Object Mode) using for obj in context.selected_objects:. This ensures changes propagate across the entire user selection simultaneously, rather than just hitting the active item.

4. Feature Creation and Extension Sequence
When asked to implement or update a function, execute these exact critical steps in order without skipping.
-Define the Properties (properties.py)
   * Open properties.py and locate the relevant property group (e.g., lsd_pg_mech_props).
   * Inject rigidly named variables using bpy.props.FloatProperty, IntProperty, etc.
   * Define strict min, max, default, and description parameters.
   * If the variable alters a mesh, bind it to an update function by setting update=update_mesh_wrapper.
   * Do not write the generation logic here.
-Construct the Generation Logic (generators.py & core.py)
   * Open generators.py.
   * Define the procedural function, ensuring it accepts context and the object being modified.
   * Initialize a clean temporary bmesh using bm = bmesh.new().
   * Extract the variables defined in Step 4A (e.g., wheel_tread_count = obj.lsd_pg_mech_props.wheel_tread_count).
   * Apply vertex changes securely using world-space matrix projections if necessary.
   * Terminate the sequence systematically: calculate normals with bmesh.ops.recalc_face_normals(bm, faces=bm.faces), write to the object with bm.to_mesh(obj.data), update the datablock with obj.data.update(), and free memory with bm.free().
-Wire the GUI Layout (panels/)
   * Locate the correct sub-file in the panels directory.
   * Append the layout.prop() function call directly to a box or row layout element.
   * Pass the exact literal variable name you created. Because the property is explicitly named and described at the properties.py level, omit the text= argument unless a specific override is required.
-Operator Deployment and Central Registration (operators.py & __init__.py)
   * If a new action command is required, build it in operators.py starting with the class name LSD_OT_Literal_Action_Name.
   * Open __init__.py. You must explicitly append the new Class to the central classes = (...) tuple at the bottom of the file.
   * Verify that the register() loop contains for cls in classes: bpy.utils.register_class(cls) and unregister() contains bpy.utils.unregister_class(cls) in reverse order to prevent ghost classes on addon reload.
   * Handler Discipline: All app handlers (depsgraph_update, load_post, etc.) must be defined in core.py.
   * Registration Safety: Always use `if handler not in bpy.app.handlers.X: bpy.app.handlers.X.append(handler)` to prevent recursive duplication during reloads.
   * Signature Compliance: Enforce the (scene, depsgraph) signature for all depsgraph handlers to ensure stability across Blender 4.5+ releases.
* Propagative Update Guards: To prevent recursive loops and feedback during multi-bone editing, you MUST use `core._prop_update_guard = True` and `core._joint_editor_update_guard = True` within operators, while wrapping the logic in try/finally to ensure they are reset. In core.py, these must be defined as module-level globals and accessed via `global` within the setter functions.

4.1 Timer-based Update Discipline (Mandatory)
To ensure Blender's stability and avoid "read-only" context errors during property update callbacks, all complex operations (mesh generation, rig updates, gizmo creation) triggered from a property update MUST be dispatched via a short-interval timer.
* Standard Pattern: `bpy.app.timers.register(lambda: bpy.ops.prefix.operator_name(), first_interval=0.01)`
* Guard Check: Always verify that the relevant update guard is False before registering the timer to prevent infinite execution loops. If the operator itself sets a guard, ensure it's properly handled in a try/finally block so code doesn't permanently freeze.

4.2 Debugging and Console Discipline (Mandatory)
When testing complex kinematic operations, the Blender System Console must be utilized for error reporting. 
* Persistent Shell: Launch Blender through the provided 'start_blender_with_addon.bat' or 'dev_tool.py' without detaching, to ensure stdout/stderr are forwarded to the primary development terminal.
* Informative Errors: Proactively log critical state changes (selection results, transformation calculated, and constraint application status) to the console to avoid the 'notification shown but nothing changed' failure pattern.
* Error Capture: Always wrap timer-based operations in broad try/except blocks to log potential context or registration errors that Blender would otherwise swallow silently.

5. Procedural Safety Limitations
 * Boolean Cut Data Purging: If a feature generates procedural cut planes or boolean objects (like Bore Holes inside gears), you must explicitly clear the previous cutter data geometry via bpy.data.objects.remove() before appending the new shape. Overlapping legacy geometry triggers Blender boolean z-fighting, resulting in corrupted, half-rendered meshes during parametric slider adjustments.
 * Real Units and Precision Scaling: Properties representing spatial dimensions (e.g., Cage Size, Part Length, Hole Depth) must explicitly include unit='LENGTH' in their bpy.props definition. This ensures Blender 4.5.6 LTS interprets them correctly as meters/millimeters in the UI. When utilizing a "Size Cage", the generation multiplier must be calculated against the precise bounding box volume (obj.dimensions) of the part's default state to ensure perfect dimensional conformity.
 * Mandatory Vertex Welding: Before finalizing any complex, nested procedural feature, you must invariably execute bmesh.ops.remove_doubles(bm, verts=bm.verts, dist=0.0001). This seamlessly welds procedural seams, closes manifold gaps, and prevents lighting artifacts and boolean stability issues downstream.

6. Code Skeleton Standards (generators.py)
To enforce memory safety and maintain structural integrity, all procedural mesh generation functions added to generators.py must strictly adhere to this architectural skeleton.
 * Type Hinting: Mandatory for context and obj parameters.
 * Data Extraction: Must pull directly from the properly prefixed PropertyGroup (lsd_pg_mech_props).
 * Memory Cycle: bmesh.new() must always be paired with bm.free() at the end of the function.

7. Naming Transition & Extension Compliance
* Previous Name: Layouts, Assembly Lines & Draftsman Toolkit
* Current Name: Layouts & Systems Draftsman Toolkit
* Repository Folder: Layouts-Systems-Draftsman-Toolkit-Addon
* Extension Module ID: layouts_systems_draftsman_toolkit (Lowercased)
* Mandatory Folder Name (in AppData/Extensions): layouts_systems_draftsman_toolkit
* All internal references must be updated to the new module name to prevent "Inconsistent name mismatch" errors during registration.

8. Current Session Tasks Log Here
115: [VERIFIED] Smart Skin Modifier: Refined the workflow into a precise 3-step process: 1. Automated Skin Modifier setup, 2. Parametric thickness control for selections, and 3. Integrated Float Curve transition mapping. Removed all non-essential UI elements to stick strictly to user specifications. Switched to `ShaderNodeFloatCurve` for a cleaner UI aesthetic.
116: [VERIFIED] Vertex Creator: Implemented a dedicated sub-panel above the Smart Skin Modifier for rapid drafting. Features a unified dropdown (Bezier/NURBS/Mesh) and a single 'Create Vertex' button format, streamlining the path generation workflow as requested.
117: [VERIFIED] Materials & Textures Overhaul: Stripping existing UI for a unified 'Paint Bucket Tool'. Implementing real-time color/texture application to selected faces with color wheel and custom texture support.
118: [VERIFIED] Marker & Kinematic Refinement: Optimized 'Attach Marker' to generate a single, object-parented handle at the origin instead of per-vertex. Exposed `joint_radius` for all active joint types in the 'Kinematic Setup' GUI and implemented an automated property sync where editing the joint radius now dynamically updates the visual gizmo radius.
119: [VERIFIED] Viewport & Extrusion Stability: Resolved a critical bug where extruding vertices caused them to become invisible or unselectable. Fixed by: 1. Optimizing the global Dimension Sync handler to avoid O(n) scene iteration during mesh editing, and 2. Refactoring Path Alignment to use BMesh for state-synchronization in Edit Mode. This ensures that extrusion previews and edges remain visible and interactive.
120: [VERIFIED] Dimensions Master Tracker (Ratio Sync): Resolved a bug where tracking existing driven dimensions would reset their ratios to 1.0. Fixed by: 1. Implementing bi-directional metadata recovery in `LSD_OT_AddToDimensionMaster` (extracting ratio from driver expressions via regex), 2. Ensuring the grouping fallback correctly recovers ratios from animation data, and 3. Enhancing `update_dimension_driver_target` to robustly resolve targets whether a Root or Label is selected.
121: [VERIFIED] Git Synchronization & Bug Fix: Resolved "unrelated histories" error by merging local state with remote GitHub repository. Fixed an `IndentationError` in `ui_kinematics.py` at line 445 (redundant conditional with incorrect indent).
122: [VERIFIED] Scaling Inaccuracy & Crash Fix: Renamed 'Gizmo Scale Multiplier' to 'Visual Gizmo Scale' in properties.py, ui_kinematics.py, and core.py. Fixed AttributeErrors in operators.py by updating stale 'gizmo_radius' references. Successfully eliminated 1000x scaling distortion and Diameter/Radius errors. Lowered MIN_GIZMO_SCALE for sub-millimeter precision.
123: [VERIFIED] Mimic System Stability (Joint Transitions): Resolved an issue where 'Add/Update Driver' failed after changing the driver's joint type. Implemented missing 'spherical' joint recognition in rotational checks across core.py, operators.py, and ui_kinematics.py. Fixed a critical unit-scaling bug in driver expressions for linear followers by ensuring physical meter results are correctly converted back to Blender Units (BU) using the scene's unit_scale, preventing 'near-zero' motion at sub-meter scales.
124: [VERIFIED] Kinematic Radius Scaling Correction: Resolved a major scaling bug where a 50mm joint radius displayed as 2 meters. This was caused by an incorrect and redundant 'unit_scale' multiplier in core.py and generators.py. Standardized all mechatronic generation to use Blender Units (BU) as meters, ensuring precision across all scene unit settings (mm/inches/m).
125: [VERIFIED] Kinematic UI Axis Alignment: Renamed the 'axis_enum' property to 'axis_alignment' and updated its UI label to 'Axis Alignment' for better clarity and compliance with the project's literal naming rules. Performed a global update across properties.py, core.py, operators.py, and panels/ui_kinematics.py.
126: [VERIFIED] Kinematic UI Modernization: Streamlined the 'Kinematic Setup' panel by removing redundant sub-panel enclosures in the Gear Ratio / Mimic section. Introduced a professional UIList (template_list) for managing existing joint couplers, replacing the previous manual iteration. Standardized the 'Joint Type' labelling within the Joint Editor Tool by explicitly naming property definitions in properties.py, ensuring a clean, CAD-compliant interface.
127: [VERIFIED] Transmission panel AttributeError fix: Resolved `AttributeError: 'LSD_PG_Kinematic_Props' object has no attribute 'transmission'` by adding the `transmission` PointerProperty to the `LSD_PG_Kinematic_Props` class in `properties.py`.
128: [VERIFIED] Physics Panel AttributeError Fix: Resolved a critical traceback in ui_physics.py where LSD_PG_Kinematic_Props was missing collision, inertial, and material property pointers. By adding these to the kinematic property group, links/bones in Pose Mode now correctly display and synchronize their physics data with the unified UI interface.
129: [VERIFIED] Kinematic UI Cleanup: Streamlined the 'Kinematic Setup' panel by removing redundant sub-panel enclosures for joint radius, visual gizmo scale, and motion limits. Renamed 'Lower' to 'Lower Limit' and 'Upper' to 'Upper Limit' in properties.py for improved CAD-standard clarity and Rule 33 compliance.
130: [VERIFIED] Installation & Scaling Stabilization: Resolved a critical `SyntaxError` in `core.py` (line 7068) and finalized the global removal of the redundant `unit_scale` factor 's' across all mechatronic generators in `generators.py`.
131: [VERIFIED] Global Property Rename Finalization: Completed the full migration from `axis_alignment` across the entire codebase (operators.py, core.py, panels). This resolves `AttributeError` tracebacks in the 'Apply Joint Settings' and 'Robot Generation' tools, ensuring 100% operational stability for the new naming convention.
132: [VERIFIED] Joint Editor Synchronization: Implemented a robust `active_bone_change_handler` with unique selection tracking (Armature:Bone) and forced UI redraw to ensure the Joint Editor immediately reflects the physical properties of selected joints.
133: [VERIFIED] Gizmo Scaling Normalization: Fixed a critical scaling bug where gizmos were physically tiny in non-metric scenes by correctly converting Meter-based `joint_radius` back to Blender Units (BU) using the scene's `unit_scale`.
134: [VERIFIED] Automatic Joint Radius Detection: Implemented `calculate_bone_mesh_radius` in `core.py` and integrated it into the `active_bone_change_handler`. The `Joint Radius` now automatically recalculates based on attached mesh geometry when a bone is selected, and these properties are instantly synchronized to the UI buffer. Removed the manual 'Read From Active' button from the sidebar as it is now redundant.
135: [VERIFIED] Spherical Joint Limit Expansion: Implemented triple-axis rotation limits (X, Y, Z) for Spherical joints. Updated core engine to apply independent ranges to both Limit Rotation constraints and IK solvers.
136: [VERIFIED] Driver Inversion Persistence: Implemented unique 'Invert' property for each mimic driver relationship. Ensured bi-directional sync between the setup form and the driver list.
137: [VERIFIED] Axis-Filtered Driver Stacking: Refactored the driver engine to support triple-axis stacking. Implemented axis filtering for 1-DOF drivers to prevent ghosting on locked axes when coupled to spherical followers.
138: [VERIFIED] Bi-Directional Ratio Propagation: Added 'Auto-Calculate Ratio' feature and implemented property propagation. Editing a driver's geometry now instantly updates all its mechanical followers throughout the rig.
139: [VERIFIED] Batch Radius Scaling Fix: Resolved bug where all joints in a batch inherited the largest target's radius. Implemented independent per-bone detection for 'Automatic' mode in the Apply operator.
140: [VERIFIED] Simplified Joint Purge: Renamed tool to 'Purge Selected Bones / Joints' and removed 'smart' preservation logic for a direct, high-speed deletion workflow.
141: [VERIFIED] Dimension Color Sync Fix: Investigating bug where arrowheads fail to sync color when 'Force Global Sync' is disabled.
142: [VERIFIED] Persistent Dimension Grouped Control: Relocated to the native Properties Editor 'Active Tool' tab (`bl_context='tool'`) as shown in the user mockup; added automatic redirection to the Properties context.
143: [VERIFIED] UI Layout Optimization: Reduced excessive gaps between collapsable sub-panels (specifically in the Presets panel) and other UI blocks by removing redundant separators and utilizing aligned column layouts (`column(align=True)`) to house adjacent boxes.
144: [VERIFIED] Geometry Nodes API Fix (4.5.6 LTS): Resolved `AttributeError: 'GeometryNodeCollectionInfo' object has no attribute 'separate_children'` in `core.py` within `setup_native_slinky`. Converted direct attribute assignments (e.g., `.separate_children` and `.count`) to explicit socket default value updates (`.inputs['Separate Children'].default_value`) to comply with modern Blender Node API.
145: [VERIFIED] Dimensions Master UI Redesign: Implemented the requested two-row layout for tracked dimensions in the 'Dimensions Master Interface'.
146: [VERIFIED] Proportional Dimension Sync: Added a `ratio` property to the master dimension items, allowing users to link dimensions with a scale factor (e.g., 2:1 or 1:2). Updated the driver logic to incorporate this ratio into the live length synchronization.
147: [VERIFIED] Electronics UI Normalization: Resolved redundancy in the 'Presets' panel by implementing a dynamic items callback for the electronics 'Type' dropdown. The list now correctly filters based on the selected category (Motor, Sensor, IC, etc.), preventing unrelated components (like resistors) from appearing in the camera category.
148: [VERIFIED] Geometry Nodes API Fix (Points to Curves): Fixed a Runtime error in 'setup_native_slinky' where 'GeometryNodePointsToCurve' was undefined in Blender 4.5+. Implemented a robust fallback to 'GeometryNodePointsToCurves' (the modern plural identifier) ensuring cross-version stability for spring generation.
149: [VERIFIED] Geometry Nodes API Fix (Slinky Sockets): Resolved `KeyError: 'Curve' not found` in `setup_native_slinky` by implementing dual-mode socket lookup (trying both 'Curve' and 'Curves'). This ensures compatibility with the pluralized socket naming convention introduced in Blender 4.0+.
150: [VERIFIED] Presets Unit Audit & Restoration: Performed a global audit of `properties.py` to ensure all spatial properties use `unit='LENGTH'` for real-world measurement compliance. Restored missing electronics properties (IC, Sensor, Camera, PCB) to the mechatronic property group, resolving UI tracebacks and enabling precise parametric control.
151: [VERIFIED] Basic Shapes Origin & Torus Fix: Implemented custom BMesh torus generation in `generators.py` to resolve `AttributeError` tracebacks. Standardized all basic shape presets (Cube, Cylinder, Sphere, etc.) to generate with their object origin at the bottom-center (lowest Z offset), improving alignment workflow.
152: [VERIFIED] Basic Shape Unit Standardization: Updated default cube dimensions to exactly 100x100x100mm (0.1m) and synchronized the parametric scaling engine in `core.py` to ensure all primitives adhere to a consistent 100mm bounding scale by default.
153: [VERIFIED] Plane & Torus Refinement: Upgraded the Plane preset to support independent 'Length X' and 'Width Y' controls for rectangular drafting. Synchronized Torus defaults with the user's reference screenshot (40mm Major Radius, 20mm Tube Radius, 40x15 segments) to ensure precision in mechatronic component generation.
154: [VERIFIED] Chain Wrapping Stabilization: Resolved the resolution-scale bug (meters vs segments) and improved the convex hull boundary extraction kernel. Chains now correctly wrap around 3D bundles with precise vertex density.
155: [VERIFIED] Mechatronic Origin Standard: All basic shapes (Cube, Cylinder, Torus, Plane, etc.) now generate with their object origin at the bottom-center (Z=0), facilitating engineering assembly.
156: [VERIFIED] Belt Preset Logic Overhaul: Fixed non-functional belt properties by implementing a smooth 'Curve to Mesh' sweep in the Geometry Nodes modifier. Added drivers for Width and Thickness to support real-time parametric adjustments.
157: [VERIFIED] Custom Hardware Override Panel: Added a 'Custom Components & Materials' sub-panel to the transmission presets. Users can now override procedural rollers and connectors with custom mechatronic assets and manage their colors directly from the GUI.
158: [VERIFIED] Advanced Chain Generation: Upgraded the default chain link mesh to include side-plates and roller pins, improving visual fidelity for non-overridden assemblies.
159: [VERIFIED] Geometry Nodes API Fix (Quadrilateral): Resolved `RuntimeError: Node type GeometryNodeMeshQuadrilateral undefined` by correcting the identifier to `GeometryNodeCurvePrimitiveQuadrilateral`. Added a robust fallback system (try/except and socket-name aliasing) to ensure stability across Blender 4.0-4.5 LTS releases.
160: [VERIFIED] Preference Panel Unification: Synchronized the 'Panel Order' feature with the unified presets architecture. Removed redundant top-level order entries for Electronics, Architectural, and Vehicle categories, consolidating them under a single 'Presets' entry. Streamlined the Preferences UI by removing secondary category visibility toggles to maintain a clean, high-fidelity drafting environment.
161: [STABILIZED] UI Registration System: Resolved a critical 'NoneType' error in panel headers by consolidating the visibility toggle operator into `ui_common.py`. Implemented a 'Zero-Waste' packaging protocol in `create_panel_addons.py` that enforces a strict whitelist for the standalone zip contents, stripping away all files and folders not explicitly required for modular functionality.
162: [VERIFIED] Smart Skin Radius Fix: Resolved `ValueError: BMVertSkin.radius: sequence length is 3, expected 2` in `generators.py`. Updated both uniform thickness and transition evaluation to use 2-element tuples (X, Y) instead of 3-element tuples, ensuring compatibility with Blender 4.5.6 LTS BMesh API.
163: [VERIFIED] Smart Skin Transition Profile Fix: Resolved issue where the transition profile curve had no effect. Corrected the `CurveMapping.evaluate` implementation in `generators.py` by: 1. Explicitly calling `.initialize()` on the mapping data-block, and 2. Updating the call signature to `evaluate(curve, position)` as required by the Blender 4.5.6 LTS API.
164: [VERIFIED] Dimensions total System Stabilization: Resolved all reported issues with the Master Tracker and Group Manager. 1. Fixed real-time synchronization via a Final Sync Sweep in `core.py`. 2. Implemented Batch Update Guards in `properties.py`, `operators.py`, and `generators.py` to preserve individual driver links during mass grouping/importing. 3. Resolved `SyntaxError` and `NameError` in the migration logic. 4. Confirmed that generic "Failed to find" console messages are harmless Blender UI notifications related to context-switching, and hardened the property-tab redirection logic for a seamless drafting workflow.
165: [VERIFIED] Materials & Textures Overhaul: Stripping existing UI for a unified 'Paint Bucket Tool'. Implementing real-time color/texture application to selected faces with color wheel and custom texture support.
166: [VERIFIED] Paint Bucket Refinement: Implemented 'Prevent Initial Fill' checkmark to avoid accidental whole-object painting on Edit Mode entry. Added automatic texture override when the color wheel is adjusted to prioritize the latest user change. Updated `properties.py`, `core.py`, and `ui_materials.py` to support these refinements. Additionally, the 'Prevent Initial Fill' toggle is now disabled by default and doubles as a feature that allows the Paint Bucket tool to apply colors and textures to entire objects directly in Object Mode just by selecting them. Added a new 'Purge Textures & Materials' toggle to the UI that instantly strips all materials from selected objects (or faces in Edit mode) when clicked.
167: [VERIFIED] Paint Bucket Bug Fix: Implemented a `_paint_bucket_hold` state in `core.py` to robustly prevent accidental full-object fills on Edit Mode entry. The tool now correctly waits for the user to make a sub-selection before applying any materials.
168: [VERIFIED] UI Cleanup: Removed the 'How to use:' help sub-panel from the Materials & Textures panel to streamline the interface.
169: [VERIFIED] Crash Fix (Paint Bucket): Resolved `EXCEPTION_ACCESS_VIOLATION` by hardening the `lsd_paint_bucket_handler` and its timer callback in `core.py`. Added comprehensive `try/except` blocks and null-context checks to prevent crashes during background operations such as file saving. Also fixed an issue where painting a single face on a mesh with no materials flooded the whole mesh by automatically appending a default grey material to the first slot.
170: [VERIFIED] SDF Boolean Panel Implementation: Created a new "SDF Booleans" panel mimicking ConjureSDF features. Implemented seamless booleans, project to surface features, and smooth adjustable welding using a Geometry Nodes based Volume-to-Mesh pipeline. Added corresponding properties and operators.
171: [VERIFIED] Directional Translate Implementation & Bug Fixes: Created a custom Modal Operator `LSD_OT_Directional_Translate` designed to replace the standard `G` translate function. It accurately calculates the translation vector based on the cursor position and freely moves objects or vertices along that exact ray when specific distance values are typed. Fixed a bug where inputting numbers caused the object to stay in place by implementing a `last_valid_direction` fallback and forcing a viewport redraw (`tag_redraw()`). Also resolved a double-registration error for `LSD_OT_Quick_SDF_Boolean`. Added a seamless keymap override (G key) in `__init__.py` that can be toggled on/off via the new `Enable Directional Translate` property in the `Workflow Optimizer` panel. Fully implemented native Middle-Mouse drag axis locking and X/Y/Z key (Global/Local) axis locking directly into the custom modal operator, ensuring a 100% seamless workflow that doesn't reset numerical input or visually jump. Additionally, added a new 'Enable Math Input (=)' toggle allowing users to enter mathematical expressions (e.g. `=5/2+1`) directly during translation. Implemented a "Smart Handoff" for the `B` key so that pressing B during directional translation seamlessly passes control to Blender's native Base-Point Snapping mode.
172: [VERIFIED] Asset Library & Workflow Optimizer: Completely removed the 'Asset Library System' panel as the workflow is too simple and can be done manually. Added a new toggle 'Asset Browser Editor' in the 'Workflow Optimizer' panel to safely allow indirect editing/naming/clearing of assets without opening their source .blend files.
173: [VERIFIED] Version Migration: Migrated the entire addon to target Blender 5.1.2 instead of Blender 4.5.6 LTS. Updated `dev_tool.py` paths and fallbacks, modified `__init__.py` bl_info, and updated comment markers in `generators.py`.
174: [VERIFIED] Asset Browser Editor Update: Added indirect asset editing capabilities below the 'Asset Browser Editor' toggle in the Workflow Optimizer panel. Created a new text input for the name and two operators (`lsd.asset_rename` and `lsd.asset_clear`). The operators smartly resolve the selected asset from the `FILE_BROWSER` space. Local assets are renamed/cleared immediately, while external assets are securely processed via a temporary background Blender instance to ensure automated saving without manual file manipulation. Added a real-time `lsd_asset_sync_timer` that natively populates the `lsd_asset_new_name` input dynamically when clicking between assets in the browser. Implemented a `lsd_asset_catalog` dropdown that parses all active Blender `blender_assets.cats.txt` library definitions to provide a comprehensive UUID-to-Catalog mapped list, and added `lsd.asset_move_catalog` operator to securely move local/external assets into specified catalogs without manually opening their files.
175: [VERIFIED] Asset Browser Editor Background Cleanup: Modified the Asset Browser Editor to directly edit the catalogs and asset naming setup for local assets, eliminating the conflicting background process system. Removed background process warning labels from the UI panel to clean up the interface.
176: [VERIFIED] Asset Browser Editor Native Implementation: Stripped out the 0.03s polling timer, custom UUID cache, and intermediary Python properties in `properties.py`. Fully refactored `ui_workflow_optimizer.py` to directly fetch and expose the selected `asset.local_id` native Blender UI properties (`name` and `asset_data.catalog_id`), eliminating all display delay and granting instant access to the exact `blender_assets.cats.txt` file tree without proxy variables. Removed obsolete renaming/moving operators from `operators.py`.
177: [VERIFIED] External Asset Direct Edit: Restored the ability to edit external assets without manually opening their files. Re-implemented the `blender_assets.cats.txt` parsing from user preferences, restored the `lsd_asset_sync_timer` (now optimized with forced redraws to eliminate UI latency), and added a `LSD_OT_Asset_Edit_External` operator. This operator utilizes a completely silent Python background process (`CREATE_NO_WINDOW`) to instantly apply names and catalog changes directly to the external `.blend` file, satisfying the requirement to edit without popup terminal windows or manual file opening.
178: [VERIFIED] External Asset Stability Fixes: Resolved a catastrophic 5-minute background process hang during external asset editing by forcing the silent process to run with `--factory-startup` (bypassing conflicting user addons) and adding a `timeout=15` hard-abort. Replaced `sys.exit()` with safe shutdown commands and secured script injection with `repr()` to prevent quote-based syntax errors. Fixed the missing catalog display for external assets by hooking into `space.params.catalog_id` from the active Asset Browser context, accurately bridging the gap where Blender's `FileSelectEntry` omits loaded `asset_data`.
179: [VERIFIED] Context Error Fix: Fixed an issue where the background asset editor successfully processed changes but failed to automatically refresh the Asset Browser due to a context incorrect error on `bpy.ops.asset.library_refresh()`. Resolved by implementing a `context.temp_override` passing the explicit `FILE_BROWSER` area and window context stored during the asset selection pass.
180: [VERIFIED] Preferences UI Update: Cleaned up the Preferences panel to correctly sync its Panel Reordering and Panel Visibility lists with the current toolsets. Added the `SDF Booleans` panel, removed the obsolete `Camera Studio & Pathing` panel, and renamed `Materials & Textures` to `Paint Tools` to accurately reflect its overhauled UI.
181: [VERIFIED] Preferences Panel True Sync: Adjusted the Preferences panel arrays and reordering data structure to perfectly mirror the active 10 toolsets in the user's environment. Purged the obsolete `Generate`, `Presets`, and `Environment & Lighting` configurations, reinstated `Camera Studio & Pathing`, and renamed `Export System` to `Import/Export System`.
182: [VERIFIED] Panel Reordering Crash Fix: Resolved an `AttributeError` trace during UI panel resorting by purging the obsolete references (`lsd_order_ai_factory`, `lsd_order_assets`, etc.) from the mapping lists within the `LSD_OT_MovePanel` and `LSD_OT_ResetPanelOrder` operators in `ui_common.py`. The operators now safely query and sort only the current active 10 panels.
