Layouts & Systems Draftsman Toolkit - AI Agent Update Workflow
2: Last Update Verification: April 02, 2026 - 11:15 AM

This workflow is the absolute source of truth for AI agents interacting with this repository: https://github.com/Japzon/Layouts-Systems-Draftsman-Toolkit-Addon.git
Follow these exact steps to update or create features while preserving the addon's framework and ensuring Blender 5.1.2 compatibility.

Category Index: {
1. Strict Operational Protocols (Mandatory)
2. Literal Naming Constraints and Structure Enforcement
2.1 Standardized Naming Conventions (Mandatory)
3. Core Architecture Rules (Strict Separation of Concerns)
4. Feature Creation and Extension Sequence
4.1 Timer-based Update Discipline (Mandatory)
4.2 Debugging and Console Discipline (Mandatory)
5. Procedural Safety Limitations
6. Code Skeleton Standards (generators.py)
7. Naming Transition & Extension Compliance
8. Current Session Tasks Log Here (Mandatory)
}

1. Strict Operational Protocols (Mandatory)
* Scope Adherence: Never add, alter, refactor, or remove features unprompted. Focus exclusively on the requested task.
* High-Density Info: Skip conversational filler. Use checklists, brief summaries, and inline code documentation.
* Task Syntax: ".," means a new isolated task. "." means a continuation/sub-step.
* 5.1.2 Compliance: Adhere strictly to the Blender 5.1.2 API. Use modern type hinting, manage BMesh memory, and pass bpy.context explicitly.
* Delete Temp Files: Immediately delete one-time helper scripts (patch_*, fix_* in _dev/) after task completion.
* Preserve Context: Operators launched from Edit Mode must restore Edit Mode upon completion (`initial_mode = context.mode`).
* No Auto-Commits: Do NOT run `git commit/push` unless explicitly requested.
* Avoid Terminals: Do not use terminals or executables to edit code; rely on direct file edits to avoid corruption.

2. Literal Naming Constraints and Structure Enforcement
* No Generic Variables: Use uncompromising, literal naming (e.g., `gear_radius`, `wheel_base_radius`). Do not reuse generic terms.
* GUI vs Properties (properties.py): Variable names must precisely match their GUI frontend labels.
* Sync GUI Classes: Panel class names must mirror their display tag and follow Blender's prefix conventions (e.g., "Mechanical Presets" -> `LSD_PT_Mechanical_Presets`).
* Trace GUI Identity (operators.py): Operator classes and `bl_idname` must logically trace the GUI text (e.g., "Generate Robot" -> `LSD_OT_Generate_Robot`, `lsd.generate_robot`).

2.1 Standardized Naming Conventions (Mandatory)
* Operators: PREFIX_OT_Description (e.g., LSD_OT_Apply_Joint_Settings)
* Panels: PREFIX_PT_Description (e.g., LSD_PT_Kinematics_Setup)
* PropertyGroups: PREFIX_PG_Description (e.g., LSD_PG_Kinematic_Props)
* UILists: PREFIX_UL_Description (e.g., LSD_UL_Mat_List)
* Scene Properties: lsd_lowercased_name (e.g., lsd_active_rig)
* Bone/Object Properties: lsd_pg_internal_props (Pointer to PG)
* Member Properties: lowercased_literal_name (e.g., joint_type)

3. Core Architecture Rules (Strict Separation of Concerns)
Never mix module responsibilities. BMesh generation in properties.py is strictly forbidden (prevents circular imports).
* __init__.py: Addon metadata, sub-module imports, and central registration loop.
* properties.py: Add-on PropertyGroup classes only.
* generators.py & core.py: 
  - Define functions accepting context and obj. 
  - Init `bm = bmesh.new()`. 
  - Extract properties. 
  - Apply vertex changes securely. 
  - End with `recalc_face_normals`, `to_mesh`, `data.update()`, and `bm.free()`.
* panels/: Append `layout.prop()` to boxes/rows. Pass the literal variable name (omit `text=` unless overriding).
* operators.py: Build action commands here (`LSD_OT_...`).
* Handler Discipline (core.py): Define all app handlers in `core.py`. Register safely: `if handler not in bpy.app.handlers.X: append(handler)`. Enforce `(scene, depsgraph)` signature.
* Propagative Guards: Use `core._prop_update_guard = True` (with try/finally block) during multi-bone editing to prevent recursive loops.

4.1 Timer-based Update Discipline (Mandatory)
Dispatch complex operations (mesh generation, rig updates) triggered from property updates via short-interval timers to avoid "read-only" context errors.
* Pattern: `bpy.app.timers.register(lambda: bpy.ops.prefix.operator_name(), first_interval=0.01)`
* Guard Check: Ensure the update guard is False before registering to prevent infinite loops.

4.2 Debugging and Console Discipline (Mandatory)
Use the System Console for error reporting during kinematics testing.
* Persistent Shell: Launch via `dev_tool.py` or `.bat` to keep stdout active.
* Informative Errors: Proactively log state changes and calculation results.
* Error Capture: Wrap timer-based operations in broad try/except blocks to log silent failures.

5. Procedural Safety Limitations
* Boolean Data Purging: Explicitly delete previous cutter geometry via `bpy.data.objects.remove()` before generating new shapes to prevent boolean z-fighting.
* Real Units: Spatial properties MUST include `unit='LENGTH'` in `bpy.props` for correct Blender 4.5.6+ scaling. Multiply by `obj.dimensions` for size cages.
* Vertex Welding: Always run `bmesh.ops.remove_doubles(bm, verts=bm.verts, dist=0.0001)` before finalizing procedural meshes to close seams.

6. Code Skeleton Standards (generators.py)
* Type Hinting: Mandatory for context and obj parameters.
* Data Extraction: Pull directly from the prefixed PropertyGroup.
* Memory Cycle: `bmesh.new()` must be paired with `bm.free()`.

7. Naming Transition & Extension Compliance
* Current Name: Layouts & Systems Draftsman Toolkit
* Repository/Extension Folder: layouts_systems_draftsman_toolkit (Lowercased)
* Ensure all internal references use the new module name to avoid mismatch errors.

8. Current Session Tasks Log Here (Mandatory)
*Note: The 20 latest tasks are logged below. Older logs are summarized.*

**[SUMMARIZED PAST LOGS]**
- Animation Layers: Blend mode defaults to COMBINE; full stack concurrent evaluation added.
- Tweak Mode & NLA: Isolated curves while evaluating full stack. Fixed infinite loops.
- Action Memory: Wipes `animation_data.action` to prevent bleeding/ghost tracks.
- API Fixes: Supported V2 Slotted Actions; removed `isolate_action`.
- Base Pose Anchor: Base layer auto-keys Frame 1 to anchor rest pose.
- Infinite Mapping: Strip bounds set to +/-100,000 for fluid evaluation.

**[LATEST 20 LOGS]**
- Animation Library Startup RestrictData Fix: Solved a benign but annoying console error where the plugin failed to load animation previews during Blender startup (`'_RestrictData' object has no attribute 'filepath'`). During `register()`, Blender aggressively restricts access to `bpy.data` because the `.blend` file hasn't finished loading into memory yet. When the preview scanner tried to read the current blend file's path to find the `anim_library` folder, it crashed. Wrapped the `bpy.data.filepath` call in a `try...except AttributeError:` block, allowing it to gracefully fallback to the temporary directory path during strict startup initialization, automatically correcting itself when the UI loads.
- Animation Onion Skin Nearest Objects Distance Fix: Solved a spatial calculation bug where selecting certain bones would fail to draw the nearest ghost meshes. Discovered that the `center_loc` for the "10 nearest objects" algorithm was hardcoded to strictly use the Armature's root object origin (`matrix_world.translation`, which is usually at 0,0,0) regardless of which bone you actually had selected in Pose Mode. If you selected a hand bone far away from the root, the engine was still scanning for meshes near the floor, completely ignoring the meshes actually near your hand. Upgraded the spatial algorithm: if the target is an Armature in Pose Mode, the engine dynamically calculates the true average world-space location of all currently selected bones (`sum(matrix @ b.head) / count`) and feeds *that* exact coordinate into the distance scanner. The nearest objects will now flawlessly orbit your exact bone selection.
- Animation Onion Skin Playback Generation Fix: Solved a logic bug where selecting bones while the timeline was actively playing would completely fail to generate new onion skins. Discovered that the "Active Transform Intercept" (which pauses generation while you drag bones to prevent lag) was being falsely triggered by playback. Because the animation physically moves the bones every single frame, the mathematical matrices of the bones were constantly shifting, which tricked the script into thinking the user was perpetually dragging the bones with their mouse. Instructed the transform intercept to strictly ignore matrix differences while `is_animation_playing` is True. The engine now instantly generates/refreshes onion skins when selecting bones even while a dense animation is actively playing.
- Animation Onion Skin Render Loop Hotfix: Solved a severe frame-stuttering crash where `draw_callback` was spamming `AttributeError: 'list' object has no attribute 'draw'` 10 times a second. During the previous decoupling update, the rendering loop accidentally maintained a reference to the raw coordinate data list in memory *after* passing it to the GPU shader compiler, instead of fetching the returned compiled `batch` object. Because it passed the raw python list directly into the GPU drawing function, the viewport engine fatally crashed every single frame. Rewired the variable references to correctly fetch the compiled shader batch directly from the updated `_cached_data` dictionary. This completely eliminates the console spam and instantly restores the real-time speed of the ghost generation.
- Animation Onion Skin Rendering Overhaul (Disappearing Meshes & Bones): Solved a severe visual clipping bug where selecting a single bone would fail to draw the rest of the armature's pose, and meshes/unselected-bones would completely vanish on keyframes. Discovered two conflicting mathematical filters: (1) In `build_onion_cache`, the `full_lines` cache was strictly intersecting the armature against `active_bone_names`. If you selected a single finger bone, the script literally only cached the finger and ignored the rest of the character. Changed the `full_lines` aggregator to unconditionally cache the entire armature (while still respecting the "must have at least one bone selected" rule), instantly restoring the full character pose context. (2) In `draw_callback`, if the engine detected a keyframe, it would completely discard the `full_lines` batch (past/future shapes) and exclusively draw the `filtered_lines` batch (the keyframe highlight). Since nearest meshes and unselected bones weren't part of the active keyframe highlight, they were entirely erased from the viewport. Rewrote the rendering loop to mathematically decouple the layers: it now simultaneously draws the `full` batch (using the faded past/future colors) AND overlays the `filtered` batch on top (using the bright green present color). This guarantees the full scene context never vanishes during playback or scrubbing.
- Animation Onion Skin State-Lock Fix: Solved a critical bug where the Onion Skin engine would permanently break and stop showing ghosts "even if bones or the entire armature were selected". Discovered that `build_onion_cache()` used a global `_is_building = True` lock to prevent recursive crashes, but the `_is_building = False` release was placed at the very end of the function, outside of any error-handling blocks. If Blender threw even a minor exception during the cache generation (e.g., when trying to delete old arrow objects from memory), the function would abort, leaving `_is_building` permanently stuck to `True`. This caused all future cache builds to instantly abort, effectively breaking the tool until a full Blender restart. Wrapped the entire core logic in a robust `try...finally:` block to guarantee the lock is released regardless of API exceptions.
- Animation Onion Skin Bone Selection & Intercept Fix: Solved a persistent bug where Onion Skins (Ghosts) refused to disappear even after the user deselected everything in 'Selected Only' mode. Discovered five overlapping edge-cases: (1) In Pose Mode, deselecting all bones does not deselect the Armature object itself, causing the engine to fallback to drawing the entire armature. Upgraded `build_onion_cache` to safely parse `context.selected_pose_bones` to extract strict bone selection. (2) The `onion_skin_timer_update`'s "Active Transform Intercept" (which prevents lag while dragging bones) was completely broken because it relied on `anim_core._ak_cache`, which had been deleted in a previous core rewrite. Because the intercept silently failed, the engine never detected when a user finished dragging a bone (dropped/keyed it), so it never knew to rebuild the Past/Future ghosts. Completely replaced the broken intercept with a self-contained `_last_bone_matrices` tracker that tracks the exact real-time matrices of all selected bones. The millisecond you drop a bone, it detects the movement stopped and automatically triggers a full `_cached_data.clear()`, instantly refreshing all ghosts without forcing the user to manually deselect the bone. (3) The ultimate cause of the ghosts persisting on deselection: `build_onion_cache` was highly optimized to only rebuild frames that were actively missing from `_cached_data` (`if f not in _cached_data`). Because deselecting a bone didn't explicitly purge the cache, the old ghosts remained in the dictionary, and the builder simply skipped rebuilding them. Implemented a real-time `_last_selection_state` hash inside the background timer that continuously monitors exact object/bone selection states. The exact millisecond the user changes selection, it triggers a forced `_cached_data.clear()`, wiping the old ghosts and perfectly rebuilding the scene. (4) Nearest Objects Ghosting: In Pose Mode, the 10 nearest objects were still drawing even when no bones were selected. Discovered the engine was populating `target_objs` with the Armature first, triggering the 10 nearest meshes algorithm, and only filtering out the empty bone selection *afterwards*. Restructured the routing loop to explicitly filter out Armatures with zero selected bones *before* evaluating `len(target_objs) > 0`, ensuring the 10 nearest objects instantly vanish when the main target is deselected.
- Animation Layer Import Playback Crash Fix (Seamless): Upgraded the import memory hot-swapping logic to allow users to import animation layers seamlessly while the timeline is actively playing. Previously, the importer instantly ripped out the active NLA strip memory to inject the new one, which crashed Blender's active background evaluation thread. Instead of forcing playback to halt, the script now uses the same robust architecture as the Rapid Delete fix: it instantly mutes and renames the active NLA track to hide it from the evaluator, creates a brand new track for the imported Action, and defers the physical deletion of the old track and action by 2.0 seconds. This allows Blender to seamlessly transition the evaluation state without crashing, maintaining uninterrupted playback. (Additionally fixed a lingering bug where the `old_action` was still being instantly deleted outside of the timer block, and fixed a naming desync where the newly created track wasn't adopting the proper UI layer name).
- Animation Layer Core Tweak-Swap Read-Only Fix: Resolved the final edge-case of the Blender 5.1 read-only `AnimData.action` crash. During layer imports, the script explicitly requested `anim_core.invisible_tweakmode_swap(exit_first=True)` to safely deposit the current layer's action into the NLA stack before constructing a new layer. This internal core function forcefully assigned `obj.animation_data.action = None`, which crashed when Blender locked the property. Wrapped all `.action` assignments throughout `anim_core.py` in `try/except` safety blocks to guarantee zero crashes during any internal evaluator swaps.
- Animation Layer Import Read-Only Crash Fix: Resolved an import failure where loading an animation layer caused the operator to crash with `bpy_struct: attribute "action" from "NlaStrip" is read-only`. Discovered that Blender 5.1 strictly locks the `.action` property of existing NLA strips. Rewrote the import routing code in `anim_library.py` to correctly extract the metadata, securely delete the old NLA strip container, and instantly spawn a brand new NLA strip explicitly linked to the imported Action, seamlessly bypassing the read-only restriction.
- Animation Layer Library Aspect Ratio Fix: Fixed a visual bug where the animated library preview was stretching into a comically tall portrait shape. The layout code was incorrectly applying a `row.scale_y = 10.0` multiplier on top of the native `template_icon(scale=10.0)` parameter, forcing Blender to stretch the row vertically while the icon attempted to maintain its aspect ratio. Removed the redundant row scale and restored the icon scale to a stable `6.0` to perfectly preserve the 1:1 square aspect ratio.
- Animation Layer Library UI Syntax Fix: Fixed a minor regression in `ui_animation.py` where opening the collapsible Animation Layer Library triggered a TypeError (`get_animated_icon_id() takes 1 positional argument but 2 were given`). The UI rendering loop was incorrectly passing the entire `item` property group and context to the animation engine, rather than just the `item.name` string that the engine expects. Corrected the function call signature and wrapped it in a safety try/except block.
- Animation Layer Tweak Mode Integration (Keyframe Visibility): Resolved a critical issue in Blender 5.1+ where selecting an animation layer threw a read-only Python error (`AnimData.action` is read-only) and failed to display the layer's keyframes in the Timeline/Dope Sheet. Rewrote the `update_active_layer` core function to abandon manual action assignment and instead programmatically inject a temporary context override that forces the NLA editor to seamlessly enter Blender's native "Tweak Mode" for the selected layer. This natively routes the keyframes to all animation editors and bypasses the read-only lock.
- Animation Layer Library UI Polish: Upgraded the Animation Layer Library sub-panel to be fully collapsible, matching the aesthetic behavior of the other panels in the system. Registered a new `library_enabled` boolean property in `LSD_PG_Animation_Settings` and updated the UI layout rendering code to utilize the standard expand/collapse toggle arrows.
- Animation Layer UI Polish & Onion Skin Selection Fix: Added the `SEQ_STRIP_DUPLICATE` icon to the Animation Layers sub-panel header and explicitly renamed the `onion_skin_mesh_resolution` property label to "Mesh Onion Skin Resolution" for clarity. Also fixed a bug in the Onion Skin targeting logic where deselecting all objects (while in 'Selected Only' mode) failed to clear the render cache, causing ghost onion skins to persist indefinitely. The engine now explicitly purges `_cached_data` and any leftover arrow objects if the target list evaluates to empty.
- Animation Layer Rapid Delete Crash Fix (Extended): Fixed the persistent `EXCEPTION_ACCESS_VIOLATION` crash that occurred when deleting animation layers rapidly. Discovered that instantly deleting the `NLA Track` memory itself (not just the action) while Blender's internal evaluator was lagging caused the core crash. Solved this by logically muting and renaming the track instantly (so it vanishes from the UI and evaluator without crashing), and explicitly deferring the actual structural deletion of the track and its actions by 2.0 seconds. Also explicitly cleared the `obj.animation_data.action` pointer if the deleted track was actively being tweaked.
- Animation Layer True Time-Based UI Playback Engine: Replaced the hardcoded, frame-indexed UI animation loop with a dynamic, metadata-driven `time.time()` engine. `update preview` now permanently records the exact `fps` and `frame_range` of the source animation into a `meta.json` file within the preview directory. The `get_animated_icon_id` engine dynamically reads this metadata, calculates the absolute real-time duration in seconds, and uses system time to perfectly synchronize the UI playback speed. This means the preview icon will always take the exact real-time duration (e.g., exactly 8 seconds) to loop, regardless of whether the user captures 5 frames or 120 frames.
- Animation Layer Update Preview Frame Bounds Corruption Fix: Solved the catastrophic bug where `update preview` always captured the maximum 120 frames regardless of the capture interval. Discovered that Blender's internal `action.frame_range` was returning massively padded bounds (up to 100,000 frames) due to NLA strip modifications, which triggered the failsafe cap. Fixed this by entirely bypassing `action.frame_range` and iterating the physical f-curves directly to extract the exact minimum and maximum `keyframe_points`, ensuring the captured bounds are perfectly accurate. Also fixed a math rounding error by using `math.ceil()` instead of `int()` during step calculation.
- Animation Layer Update Preview Smoothness Fix: Removed the hardcoded `0.1` interval failsafe and restored `max_capture_frames` to `120` in `anim_library.py`. This allows users to set a much lower capture interval (e.g. `0.04`) to correctly capture high-framerate, smooth animated previews without the script artificially clamping the frame count to 16.
- Animation Layer UI Cleanup: Removed the 'ADD' blend mode option from the `blend_type` and `import_blend_type` dropdowns in `properties.py` to hide it from the UI while keeping the underlying system intact and safe.
- Animation Layer UI Playback & Combine Fix: Uncoupled the animated icon UI timer from the static `preview_capture_interval` and synced it directly to `1.0 / scene.render.fps` so that the preview correctly plays at the timeline's real-time speed. Enforced exactly `16` max capture frames. Fixed a mathematical teleportation bug in COMBINE mode by zero-basing the True Delta Engine using `fc.evaluate(min_frame)` instead of relying on incorrectly offset raw keyframe coordinates.
- Animation Layer Update Preview Mute & Bounds Fix: Fixed `update preview` evaluating the base layer's infinite bounds (+/- 100,000) instead of the actual keyframe bounds by directly reading `action.frame_range` and calculating the NLA offset. Also added an explicit `context.view_layer.update()` after muting tracks to ensure the OpenGL render instantly isolates the correct layer poses.
- Animation Layer Update Preview Optimization: Reduced `max_capture_frames` from 120 to 24 (and now 120 again for smoothness) in `anim_library.py` to significantly speed up preview capturing. Removed temporary file creation (`debug_preview.txt`) to clean up after completion.
- Animation Layer True Delta Engine (Smooth Transitions): Replaced the absolute spatial baking system with a mathematically correct True Delta Engine in `anim_library.py` to fix overlapping jumping and snap issues.
  1) **4D Quaternion Deltas**: To prevent the "wild spinning" caused by linear F-Curve manipulation, the script now accurately evaluates the W,X,Y,Z quaternion curves as 4D mathematical objects, calculates the true delta `(current_q @ base_q.inverted())`, and dynamically writes the uncorrupted true delta back into the timeline. 
  2) **COMBINE Mode Lock**: Because the engine flawlessly zero-bases all imported animations (including complex Quaternions), the `COMBINE` blend mode now performs perfectly smooth, zero-snap addition.
  3) **True Transitions**: By combining true Delta actions with the 8-frame `blend_in`, the add-on seamlessly crossfades between layers, creating a perfect, fluid transition from the end of one layer to the beginning of the next without jumping or sliding.
- Animation Layer Import Flatline & Bounds Fix: Re-applied the infinite NLA bounds (`-100000`) and disabled `use_sync_length` immediately after assigning an imported action to the NLA strip in `anim_library.py`. To align the imported poses to the playhead without corrupting the F-Curves, the script now mathematically shifts the NLA strip's `frame_start` and `frame_end` by the playhead offset instead of mutating the raw keyframe coordinates. This completely eliminates the "flatline" corruption and perfectly evaluates the poses.
- Tweak Mode Concurrency: Replaced `isolate_action=True` with `False` during tweak enter/exit, allowing all underlying layers to evaluate concurrently like Maya.
- Infinite Mapping Fix: Disabled `use_sync_length` and set `frame_start/end` + `action_frame_start/end` to +/- 100,000 to bypass Tweak Mode bounds freezing. Removed 'Add' blend mode.
- Floating Bones Fix: Switched default blend mode to REPLACE to fix additive double-transforms on connected bones in COMBINE mode.
- UI Restructure & Reversion: Reverted default blend to COMBINE per user request. Grouped Animation Layers under `LSD_PT_Animation_System_Main` subpanels.
- Master UI Sorting Integration: Removed `bpy.types.Panel` from `LSD_PT_Animation_System_Main`, making it a drawing helper integrated into `ui_main.py` to respect master sorting.
- Hardcoded Sort Fix & Blend Purge: Added animation panel index to hardcoded sorting logic in `ui_common.py`. Committed `COMBINE` default fix.
- UILayout Spam Fix: Created placeholder operators in `operators.py` for missing boolean SDF tools to silence UI redraw exceptions.
- Layer UI Cleanup & Influence Fix: Removed extra tools sub-panels. Renamed Bake operator. Updated influence slider logic in `anim_core.py` to recursively apply to all scene objects globally.
- Global UI Hierarchy & Onion Skin UI: Reordered master UI panels to match user's screenshot. Added `Timeline Onion Skinning` UI placeholder in `ui_animation.py`.
- Onion Skin UI Implementation: Fixed icon `TypeError`. Added variables for frames before/after, distance, opacity, and custom ghost colors to `properties.py` and UI.
- Refresh Layer & Onion GPU Logic: Added a 'Refresh' button mapped to `invisible_tweakmode_swap`. Built core GPU overlay framework in `anim_onion_skin.py`.
- Onion Skin GPU Optimization & Auto-Refresh: Bound ghost cache builder to `bpy.app.timers` for real-time live updates. Replaced `mesh.edges` with `obj.bound_box` for speed. Added Mesh Resolution decimation slider and proximity toggle.
- Onion Cache Fading & Partial Builds: Optimized cache to only build missing frames instead of full stack. Added "Fade Outer Ghosts" to linearly drop opacity. Tuned defaults.
- True Edge Outlining: Refactored mesh outlining to only highlight the top 95% spatial distance edges (extreme silhouettes) in a secondary color. 
- Global Range Sync: Forced ghost evaluation to respect scene `frame_start` and `frame_end`.
- Outline Refinement: Switched from percentage threshold to exact 'N' edge count. Fixed UI color wheel interpolation issues.
- Outline Removal & Color Update: Removed complex outline logic due to visual overlap. Restored single-pass rendering. Updated ghost colors to Magenta/Green.
- Computational Caching & Colors: Cached `batch_for_shader` directly in memory to stop 60fps GPU reallocation. Optimized Python vertex loops in C. Updated ghosts to Pale Yellow/Blue.
- Ghost Infinity Bug Fix: Fixed vertex vector reference corruption by extracting pure Python tuples before calling `to_mesh_clear()`.
- Toggle Freeze & Opacity: Fixed bug where disabling layers trapped the user in Tweak Mode. Added auto-unlock block. Set default opacities to 0.1.
- DepsGraph Selection Fix (Color Purge): Fixed dimension color resetting by blocking recursive depsgraph material updates. Deferred color writes via a 50ms timer outside depsgraph.
- Auto-Key Engine Debounce Fix: Root-caused failed auto-keying; `active_operator` was permanently blocking the engine.
- Auto-Key Engine Rewrite (Polling): Scrapped depsgraph engine for a `bpy.app.timers` 200ms polling loop. Evaluates bone `matrix_basis` diffs, ensures slotted actions exist, then dual-pass keys loc/rot/scale.
- API 5.1 Compatibility: Patched `bone.bone.select` crashes with a fallback `try-except` block to `bone.select`.
- Slotted Action Snap-back Fix: Fixed NLA strips ignoring newly created layers. Dynamically generates `.slots.new(for_id=obj)` for all empty actions before assigning them to Tweak Mode.
- Auto-Refresh Race Condition Fix: Protected un-keyed poses from being wiped out by the background onion skin timer. Injected a `matrix_basis` diff guard that suspends ghost updates if a bone is actively being posed.
- Auto-Key Empty Layer Snap-back & Bleeding Fix: After exhaustive testing, all native C++ `bpy.ops.anim.keyframe_insert` operators have been completely nuked from the Auto-Key Engine. They proved far too unreliable for Blender 5.1's new Slotted Actions, frequently failing silently due to UI context constraints and causing extreme console spam. The engine now exclusively uses the Python API `bone.keyframe_insert()`. Since Tweak Mode is always active when animating layers, `bone.matrix_basis` inherently holds the perfectly calculated inverse NLA delta. Additionally, discovered a critical bug where Blender 5.1 silently rejected `action.slots.new(for_id=obj)` unless `name` and `id_type` were explicitly provided, causing F-Curve creation to fail entirely on empty layers. This slot initialization bug has now been successfully eradicated across the entire codebase. The engine also now strictly respects the 'Automatic Keyframe Everything' toggle by leveraging `options={'INSERTKEY_NEEDED'}`.
- Onion Skin Alternating Keyframe Colors: Added a second color picker to the Timeline Onion Skinning panel ("Onion Skins with Keyframes Color 2"). The rendering engine now automatically sorts the active keyframes chronologically and alternates between Color 1 and Color 2 for each ghost, making dense clumps of keyframes drastically easier to visually differentiate.
- Automatic Keyframe Everything Fix: Fixed a logic bug in `core.py` where the auto-keyframe engine was exclusively keying `selected_bones` even when the 'Automatic Keyframe Everything' toggle was enabled. This caused unselected bones to lose their poses (snapping back to NLA defaults during timeline scrubs) which resulted in ghosts showing a misaligned pose compared to the actual playback. The engine now correctly keys all bones when `use_auto_all` is enabled.
- Tweak Mode Bounds & Rotation Mode Conflict Fixes: Fixed an issue where onion skins showed correct poses while the bones generated distorted interpolations ("made up their own poses"). 1) Fixed `anim_core.py` where existing NLA strips were incorrectly mapped to `use_sync_length = True`, causing Tweak Mode bounds to freeze and fail extrapolation. 2) Fixed `core.py` unconditionally keyframing both `rotation_quaternion` and `rotation_euler` on all bones during auto-keyframing. The engine now explicitly reads `bone.rotation_mode` and only keys the active rotation type to prevent F-Curve cross-contamination.
- Infinite Bounds Offset Destruction & Rig Constraint Fix: Solved the final issue where existing poses were teleporting out of sync with ghosts. 1) Reverted the `core.py` Auto-Key loop that erroneously keyed all unselected bones (including IK and Twist deformation bones) which broke native constraints during playback. 'Automatic Keyframe Everything' now correctly maps to `INSERTKEY_NEEDED` on actively posed bones. 2) Fixed a critical bug across `anim_core.py` and `operators.py` where expanding NLA strips to infinite bounds (`-100000`) forcefully erased their timing offsets, causing playback to desync from the F-Curve ghost cache. The scripts now calculate and apply the `orig_offset` mathematically when mapping the timeline to infinity.
- Slotted Action Name Mismatch Fix: Fixed a Blender 4.3+ API bug where new keyframes would fail to insert (logging `Could not insert key... for slot OB[Name]`). The internal slot creation logic across `core.py`, `anim_core.py`, and `operators.py` was generating slots using `obj.name` ("Armature.001"), while Blender's internal C evaluator requires the full block ID name ("OBArmature.001"). Updated all `slots.new()` calls to explicitly map to `obj.id_data.name`, fully restoring manual and automatic keyframing on newly generated layers.
- Auto-Key NLA Time Teleportation Fix: Solved the critical bug where inserting new keyframes would inexplicably overwrite/destroy existing keyframes on other frames ("made up poses"). The Python API `bone.keyframe_insert()` was defaulting to the raw Scene Frame, completely ignoring the NLA strip offsets. If a user posed at Scene Frame 500 on an offset layer, it would inject the keyframe into Action Frame 500, which Tweak Mode would then teleport to evaluate at Scene Frame 950! `core.py` now mathematically calculates the `target_frame` by reverse-engineering the NLA offset and forces the keyframe into the perfectly synchronized Action time space.
- Auto-Key Engine Replacement & Target Ghost Filtering: The "Automatic Keyframe Everything" engine was completely removed from the add-on at the user's request due to usability issues. In its place, a manual "Keyframe Entire Pose" button was implemented in the Animation Layers UI (`LSD_OT_Anim_Keyframe_Entire_Pose` in `operators.py`), which uses the mathematically-sound NLA target frame offset calculation. Additionally, a new Keyframe Filter dropdown was added to the Onion Skin UI. When set to 'TARGETS' (the new default), the Onion Skin engine (`anim_onion_skin.py`) aggressively parses F-Curve data paths to identify exactly which bones are keyframed on a given frame, and visually filters the "Present" ghost highlight so that only bones with actual keyframes are rendered in the ghost mesh.
- Manual Keyframe Teleportation & Overwrite Fix: Solved an issue where the new "Keyframe Entire Pose" button was incorrectly parsing the active NLA track via `track.active` (which evaluates to False in the Python API unless explicitly assigned), causing all manual keyframes to be injected at the raw Scene time instead of the offset Action time. This caused Tweak Mode to evaluate outside the keyframe bounds and extrapolate only a single pose across the timeline ("only showing the latest pose"). The target_frame logic now safely evaluates `strip.action == obj.animation_data.action` to guarantee the exact offset is found. Also restored custom property keyframing to the manual button to ensure facial rigs and sliders are properly saved.
- Layer Blending Override Fix: The "Keyframe Entire Pose" button previously forced keyframes onto all bones regardless of selection, completely overriding underlying animation layers in 'REPLACE' mode. Modified the operator to only keyframe `context.selected_pose_bones` or `context.selected_objects`, strictly enforcing selection to allow independent layered animations to blend correctly. Also updated the button label to "Keyframe All Transforms for Selected" and extended support to Object mode so entire object transforms can be layered seamlessly.
- Onion Skin Live Refresh Jitter Fix: The previous fix to `onion_skin_timer_update` aggressively rebuilt keyframed ghosts by internally calling `context.scene.frame_set`, which violently interrupted live user transforms (dragging/rotating), causing extreme jitter. Reverted the timer to only invalidate the current frame's cache. Keyframed ghosts correctly update without jitter when explicitly invoking the "Calculate Ghosts" button.
- Onion Skin Idle-Based Full Refresh: Added a new `onion_skin_depsgraph_handler` to track when the user modifies a pose, and updated the `onion_skin_timer_update` to intelligently trigger a full cache flush and rebuild *only* after 0.3 seconds of idle time. This ensures all target ghosts reliably update to reflect the newly interpolated poses without jittering or interrupting active mouse drags.
- Onion Skin Layer-Switch Synchronization Fix: Discovered a bug where the onion skin ghosts (and their highlighted keyframe colors) would fail to update when the user selected a different animation layer in the UI list. Because swapping layers via `invisible_tweakmode_swap` didn't always flag the depsgraph as explicitly dirty for the onion skin handler, the ghost cache from the previous layer would remain stuck on the screen, showing incorrect keyframes or disappearing entirely. Injected an explicit cache wipe (`_cached_data.clear()` and `_cached_keyframe_frames.clear()`) directly into the Tweak Mode swap engine. Now, the moment the user selects a new layer, the onion skin instantly flushes its cache and seamlessly rebuilds the ghosts perfectly mapped to the newly selected layer's keyframes.
- Viewport Layer-Switch Refresh Fix: Fixed a visual bug where switching to a different animation layer would sometimes leave the viewport stuck displaying the poses of the previous layer until the user manually triggered an update (e.g., by toggling the Mute button). Added a forced `context.view_layer.update()` and explicitly tagged the character object for `{'OBJECT', 'DATA'}` refresh immediately after entering Tweak Mode, guaranteeing the 3D viewport instantly snaps to the poses of the newly selected layer.
- Automated Layer Sync (Auto-Refresh): Completely automated the "Refresh Layer Sync" functionality. Because Blender's NLA Tweak Mode sometimes struggles to immediately visually evaluate newly inserted keyframes against a 'COMBINE' layer stack, the user previously had to manually press the "Refresh Layer Sync" button after posing to see the final result. Initially, this automation was accidentally gated behind the Onion Skin system, meaning it stopped working if Onion Skin was disabled. Restructured the timer logic to decouple the Layer Sync from the Onion Skin loop. Now, exactly 0.3 seconds after the user stops dragging a bone or editing a pose, the script globally and silently synchronizes the NLA stack in the background, guaranteeing that the poses are perfectly blended without requiring any manual UI clicks, regardless of whether Onion Skins are enabled or disabled.
- Onion Skin Playback Lag Fix: Discovered a catastrophic performance regression where decoupling the automated Layer Sync accidentally broke an `elif` condition protecting the Onion Skin cache during timeline playback. Because the condition was broken, the script was forcing a full `frame_set()` calculation on the current frame every 0.1 seconds while the animation was playing, dropping performance from 25 FPS down to 2 FPS. Restored the `not _is_depsgraph_dirty` check to the Onion Skin evaluation loop. This correctly skips the aggressive cache rebuilds while the timeline is playing, instantly restoring real-time performance.
- Onion Skin Playback Visibility & Performance: Removed the playback abort intercept and initially injected a strict "Dynamic Throttle" (2 frames per tick) into `build_onion_cache`. However, this throttle caused future frames and keyframe ghosts to be permanently starved and delayed (they were truncated from the build list and never evaluated before the user scrubbed again). To solve this, the dynamic throttle has been completely removed. Instead, the timer safely aborts cache rebuilding *exclusively* during timeline playback (`is_animation_playing`). This entirely prevents the 2 FPS playback stutter (because no `frame_set()` occurs during play), while allowing ALL ghosts (past, future, and keyframes) to instantly evaluate in a single, fast burst the exact millisecond the user pauses or scrubs.
- Onion Skin Infinite Cache Destruction Loop Fix: Discovered that the dynamic throttling feature caused the ghosts to appear very faint and constantly refresh. This was happening because the `frame_set()` commands used inside the cache builder to evaluate the ghosts were triggering Blender's global dependency graph handler, which was falsely detecting them as "user edits". This false detection triggered the Auto-Sync timer to violently delete the entire ghost cache every 0.3 seconds in an infinite loop. Wrapped the internal `frame_set()` calculations inside `build_onion_cache` with a strict `_is_auto_syncing` intercept flag. This blocks the dependency graph from wiping the cache during generation, resulting in rock-solid, stable ghosts that successfully evaluate over time.
- Automated Layer Sync Rollback (UI Destructive Behavior): Discovered that running the automated "Refresh Layer Sync" via a background timer was inherently destructive to Blender's UI. Because `invisible_tweakmode_swap` invokes `bpy.ops.nla.tweakmode_enter()`, forcing this operator in the background violently interrupted the user's active UI context, causing sliders (like the Onion Skin Opacity slider) to instantly deselect while dragging. Furthermore, dragging a UI slider naturally triggers the dependency graph, which caused the Auto-Sync timer to immediately trigger the Tweak Mode swap, instantly destroying the slider interaction. The global background Auto-Sync Layer feature has been fully reverted and ripped out of the timer to restore UI stability. The user must use the manual "Refresh Layer Sync" button for NLA 'COMBINE' evaluation to avoid destroying real-time UI interactions.
- Onion Skin Keyframe Ghost Fix: Discovered that the logic responsible for parsing F-Curves and extracting keyframes (`_cached_keyframe_frames`) was accidentally locked behind the `onion_skin_show_present` UI toggle. This caused all Keyframe Ghosts to completely disappear when the user turned off the "Show Keyframes" ghost toggle, permanently breaking the cache. Uncoupled the F-Curve parsing from the UI toggle so that keyframes are always mathematically evaluated and successfully injected into the ghost pipeline regardless of UI state.
- Automated NLA Cache Invalidation (Mute/Unmute Workaround): The user discovered a severe internal Blender bug where rapidly switching between Animation Layers caused the viewport to physically freeze on the previous layer's poses, ignoring both 'Refresh Layer Sync' and structural updates. The only way the user could fix this was by manually clicking the Mute/Unmute UI toggles on the layers to force Blender to register the poses again. To fix this permanently, injected a programmatic loop into `invisible_tweakmode_swap` that rapidly inverts and restores the `.mute` property on every single NLA track in the background immediately after entering Tweak Mode. This forces Blender's C++ back-end to entirely dump and rebuild its NLA stack cache, guaranteeing the viewport instantly and flawlessly registers the correct poses every time the user clicks a different layer.
- Animation Layer Library & Animated UI Previews: Implemented a highly requested UI feature that acts as a centralized repository for saving and loading animation layers. Because Blender's Python UI API does not natively support MP4 video playback in `UIList` layout elements, I constructed a custom "Animated Icon Engine". When a layer is exported, the script commandeers the viewport and captures a fast sequence of OpenGL screenshots across the Action's keyframes. This image sequence is loaded into a custom `bpy.utils.previews` collection. A background timer running at 10 FPS iterates through the images and updates the UI's `template_icon`, creating a seamless, real-time "video preview" of the animation layer directly inside the Blender panel without risking BGL/GPU crashes. Layers are exported as native `.blend` files to preserve all Blender-specific Action F-curve data.
- Animation Layer Blend Type Sync: Fixed a bug where changing the `Blend` enum property (Replace vs Combine) in the UI would not propagate to an existing NLA strip. `anim_core.py`'s `execute_sync_logic` now explicitly forces `existing_strip.blend_type = active_layer.blend_type` so that layer inheritance correctly updates in real-time.
- Animation Layer True Deletion Fix: Discovered that `LSD_OT_Anim_Layer_Remove` was only deleting the layer from the UI list `settings.layers` and was completely failing to delete the underlying NLA track or the Action block from the `.blend` file. This caused deleted keyframes to persist secretly and reattach to newly created layers. Completely rewrote the operator to iterate through all scene objects, surgically remove the associated NLA track, and call `bpy.data.actions.remove(action)` to permanently annihilate the layer's keyframe data when deleted.
- Snap To Keyframe Teleportation Fix: The "Snap to Frame with Keyframes" operator (`LSD_OT_SnapToKeyframe`) was reading raw un-offset Action frames from the F-Curves and teleporting the user's Scene frame back to those raw integers, completely ignoring the NLA strip offset. Updated `LSD_OT_SnapToKeyframe` to calculate the exact `strip.frame_start - strip.action_frame_start` offset and re-map the snapped frames perfectly back into the global Scene time space.
- Snap To Keyframe Performance & Playback Upgrade: The "Snap to Frame with Keyframes" operator previously felt very slow because it was looping through thousands of keyframe points using standard Python `for` loops. Rewrote the extraction engine to use Blender's C-level `foreach_get` array API, making the keyframe scanning practically instant. Additionally, added logic to detect if the timeline is currently playing. If it is playing, the operator now safely pauses the internal clock, snaps the frame, and seamlessly resumes playback, allowing the user to snap around the timeline perfectly without having to stop the animation.
- Snap Backwards (Playing) Skip Fix: Added a QoL fix for snapping to the past keyframe while the timeline is playing. Previously, because the timeline constantly moved forward, snapping backwards would instantly push the playhead past the keyframe again, trapping the user in a loop where they could only snap to the exact same keyframe. The "Snap to Past" operator now detects if the timeline is actively playing and automatically skips the *immediate* nearest past keyframe, dropping the user on the *second* past keyframe instead. This allows the user to continuously snap backwards through the animation while it's playing without getting stuck.
- Snap Precision Offset Fix (Off-By-1): NLA math relies on floating-point time data (`strip.frame_start` etc.). Discovered that during snapping and keyframe generation, these float time-offsets were causing precision inaccuracies that would insert keyframes on sub-frames (e.g. `10.999` instead of `11.0`). Added strict `round()` wrappers around all NLA offset calculations in `operators.py` so that keyframes perfectly align with global integer scene frames. Additionally, fully implemented the `strip.scale` interpolation mapping in both operators to ensure mathematically flawless NLA conversions even when users aggressively re-time their layers.
- Keyframe Overwrite Snapping (Float-Drift Fix): Discovered that when using Blender's native keyframing, Tweak Mode perfectly preserves NLA float drift, which causes `bpy.ops.anim.keyframe_insert` to spawn a new keyframe at a subframe (e.g. `5.5`) right next to the original target keyframe (e.g. `5.0`). Rebuilt `LSD_OT_Anim_Keyframe_Entire_Pose` to post-process the F-Curves instantly after keyframing: if a float duplicate is detected, it securely deletes the original keyframe and surgically snaps the newly generated inverse-NLA keyframe down into its place.
- Combine Blending Initialization Fix: The "Combine" layer blend mode was visually acting like "Replace" upon creation, only working after manually switching layers back and forth. Discovered that when a new NLA strip is created via Python and assigned a 'Combine' blend mode, Blender caches its default 'Replace' state in the depsgraph. If `tweakmode_enter` is called immediately afterward without a depsgraph update, Tweak Mode fails to initialize the inverse-NLA math engine. Injected a forced `context.view_layer.update()` inside `invisible_tweakmode_swap` right before Tweak Mode execution, instantly solving the issue.
- Infinite Bounds Restoration (NLA Time Shift Fix): Restored the `frame_start` and `frame_end` to +/- 100,000 (infinite) bounds on all generated NLA strips, and forcibly disabled `use_sync_length`. Previously, the add-on attempted to use localized layer bounds (`use_sync_length = True`), but this caused a severe Blender NLA engine bug where inserting a keyframe outside the action's existing range dynamically resized the action, mathematically dragging the strip's `frame_start` along with it. This caused catastrophic time-desynchronization (keyframes being inserted on the wrong frames in the timeline) and caused layers to "forget" poses because `extrapolation = 'NOTHING'` froze the evaluation outside the strict local bounds. The infinite bounds guarantee the timeline remains perfectly anchored and allows poses to be held across the entire timeline without snapping back.
- Inverse NLA Scale Corruption Fix: Discovered a catastrophic flaw in how the previous infinite bounds hack was applied in Python. Setting `strip.frame_start = -100000` BEFORE setting `strip.action_frame_start = -100000` on an empty action caused Blender's internal engine to divide by zero and permanently destroy the `strip.scale`. Because the scale was corrupted, Blender's Tweak Mode failed to calculate the inverse-NLA mapping. This caused any inserted keyframes to silently reject the NLA math and blindly duplicate the Base Layer's absolute transforms, making it appear as if the poses were being deleted or violently snapping to the first layer's position. Fixed the assignment order globally and explicitly forced `strip.scale = 1.0` during layer initialization, ensuring perfect mathematical Tweak Mode stability.
- Layer Name Collision (Shared Keyframes) Fix: Fixed a critical bug in `LSD_OT_Anim_Layer_Add` where deleted layers would cause the `layer_count` to reuse names (e.g., naming a new layer "Layer 2" when "Layer 2" already existed). This caused Blender to secretly name the internal NLA track "Layer 2.001", which tricked the `anim_core.py` Tweak Mode swapper into activating the old "Layer 2" track instead. Overhauled layer creation to iterate and guarantee strictly unique naming, ensuring brand new layers are truly blank slates.
- NLA Time Shift (Pose Deletion) Fix: Fixed a critical bug in `LSD_OT_Anim_Keyframe_Entire_Pose` where new poses done in animation layers kept getting deleted (teleporting out of sync). When keyframing an empty or localized action, `use_sync_length = True` aggressively changed the action bounds and shifted the `action_frame_start`, which maliciously dragged `frame_start` along with it and destroyed the time sync. The operator now mathematically captures the `orig_offset` before keyframing and surgically restores `strip.frame_start` after insertion, perfectly preserving localized layer bounds while guaranteeing time sync.
- Removed Influence UI & Tweak Mode Toggle: Per user request, the Influence slider and the "Animate Layer (Tweak Mode)" toggle have been completely removed from `ui_animation.py` and `properties.py`. The system has been reverted back to strictly managing the `Blend Mode` property natively, simplifying the UI.
- Layer Muting & Evaluation Logic Fix: Found a severe bug in `anim_core.py` where locking a layer automatically forced its NLA influence to `0.0` and muted its track. Since the Base Layer is locked by default, it was being silently deleted from the Tweak Mode evaluation stack, which caused the rig to snap back to the raw rest-pose when animating on top of it. Separated the `layer.is_locked` checks from the muting logic so that locked layers still evaluate perfectly. Additionally, made the Base Layer automatically muted upon creation as requested.
- Onion Skin Muting Fix: Updated `anim_onion_skin.py` to actively skip F-Curves that are muted, ensuring that ghosts immediately disappear when an animation layer is muted.
- Influence UI Accessibility Fix: Moved the layer Influence slider into the main UI List `LSD_UL_Animation_Layers` instead of keeping it trapped under the active layer properties. Because Blender's Tweak Mode inherently forces the *active* layer's influence to 1.0 (so you can accurately animate it), the slider appeared completely broken when adjusted. Now, users can adjust the influence of *non-active* layers and see the smooth blending results immediately in the viewport.
